import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import { CALENDAR_URL } from '../data/content';
export default function Header({t,lang,setLang}){const[open,setOpen]=useState(false);return <header className="header"><a className="brand" href="#inicio"><img src="/images/logo.png" alt="Paola Leite Eikeset"/></a><nav className={open?'nav open':'nav'}>{t.nav.map((x,i)=><a key={x} href={['#inicio','#especialidades','#nordicos','#publicacoes','#contacto'][i]} onClick={()=>setOpen(false)}>{x}</a>)}</nav><div className="actions"><button className="lang" onClick={()=>setLang(lang==='pt'?'en':'pt')}>{lang==='pt'?'PT | EN':'EN | PT'}</button><a className="button small" href={CALENDAR_URL} target="_blank" rel="noreferrer">{t.book}</a><button className="menu" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button></div></header>}
