import { articles } from '../data/content';
export default function Article(){const slug=location.pathname.split('/').filter(Boolean).pop();const a=articles.find(x=>x.slug===slug);if(!a)return <main className="article-page"><a href="/">← Voltar</a><h1>Artigo não encontrado</h1></main>;return <main className="article-page"><a href="/">← Voltar ao início</a><p className="section-label">{a.category}</p><h1>{a.title}</h1><p className="lead">{a.excerpt}</p><p>Este espaço está preparado para receber o texto integral do artigo. O conteúdo pode ser acrescentado diretamente no ficheiro <code>src/data/content.js</code> ou numa estrutura própria para publicações.</p><a className="button" href="/#contacto">Agendar consulta</a></main>}
